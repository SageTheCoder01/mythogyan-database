# ============================================
#  MythoGyan Graph Sync Configuration
# ============================================

# 🔹 PostgreSQL Connection
PG_CONN=dbname=mythogyan user=postgres password=mytho_pass host=localhost port=5432

# 🔹 Neo4j Connection
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASS=neo4jpass

# 🔹 Logging and Runtime
LOG_FILE=graph_sync.log
LOG_LEVEL=INFO

# 🔹 Sync Behavior
# FULL — rebuild entire graph each time
# DELTA — only sync changed records (future enhancement)
SYNC_MODE=FULL

# 🔹 Notification (optional future use)
ENABLE_SLACK_ALERTS=false
SLACK_WEBHOOK_URL=
