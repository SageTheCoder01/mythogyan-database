# ============================================
# MythoGyan Graph Sync Configuration — Enhanced
# ============================================

# 🔹 PostgreSQL Connection
PG_CONN=dbname=mythogyan user=postgres password=mytho_pass host=localhost port=5432

# 🔹 Neo4j Connection
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASS=neo4jpass

# 🔹 Logging
LOG_FILE=graph_sync.log          # Path to log file
LOG_LEVEL=INFO                   # DEBUG, INFO, WARNING, ERROR
LOG_ROTATE_DAYS=7                # Keep logs for X days

# 🔹 Sync Behavior
SYNC_MODE=FULL                   # FULL = complete rebuild, DELTA = only changed records

# 🔹 Notifications (optional)
ENABLE_SLACK_ALERTS=false
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# 🔹 Delta Sync (future use)
DELTA_TRACK_TABLE=graph_sync_status    # Table tracking last sync timestamp
DELTA_BATCH_SIZE=500                   # Number of nodes/edges per batch

# 🔹 Misc
RETRY_COUNT=3                          # Retry on failure
RETRY_DELAY_SECONDS=10
